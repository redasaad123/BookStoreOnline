@if (Model.IsClickedInBasket == true)
{
    <a class="btn btn-primary w-75 mt-3 Added "> Added </a>

}
else
{
    <a class="btn btn-primary w-75 mt-3 js-PostInBasket " href="javascript:;" data-id="@Model.Id">Add to Basket</a>
}
